{
  "name": "mini-internal-developer-platform-app",
  "version": "1.0.0",
  "description": "Sample Node.js application deployed by the mini internal developer platform.",
  "_comment": "JSON does not support native comments, so this metadata key documents file intent.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node -e \"console.log('No automated tests configured yet')\""
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
